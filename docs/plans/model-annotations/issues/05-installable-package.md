# 05：验证打包产物在独立 Vue 项目中可用

**What to build:** 开发者安装打包后的 npm 产物，能够加载模型、使用标注与模式切换，并获得完整样式和类型支持。

**Blocked by:** 03：用 A 侧栏编辑并切换课堂展示；04：旋转与遮挡时保持标注可读。

**Status:** complete

**GitHub:** https://github.com/OpenEdu3D/openedu3d-viewer/issues/7

- [x] 修复类型声明产物缺失问题，所有声明的包导出对应实际文件。
- [x] 提供公开样式导入入口和自包含默认样式，接入项目无需原产品的全局变量或布局类。
- [x] 公开组件、模型与标注类型、事件和绑定在消费者类型检查中可用。
- [x] 保持现有公开导出可用；原型路由、调试状态和变体切换不进入正式公开入口。
- [x] 通过 npm 打包产物安装到独立 Vue 项目，验证渲染、样式、标注数据绑定、编辑与展示和生产构建。
- [x] 组件的浏览器初始化在挂载后进行，包导入不依赖已存在的浏览器全局对象。
- [x] 从干净安装执行类型检查、构建及相应行为测试，记录消费者验证结果。

验收记录见 [validation.md](../validation.md)。

## Parent

正式规格 #2；原型 primary source 见 GitHub Issue #1。
