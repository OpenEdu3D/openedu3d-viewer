# 三维模型教学标注实现计划

已确认的范围：A 侧栏布局，侧栏可隐藏；编辑与只读课堂展示模式；URL 与本地静态未压缩 GLB；表面锚点、引线及可读文字；开发者传入和获取标注数据。先完善组件，再完善开发者上手。

完整规格见 [spec.md](./spec.md)。

已确认主要验收入口：开发者使用的公开 Vue 输入、双向绑定和事件，配合真实浏览器中的 canvas 交互。最后从 npm 打包安装后的独立 Vue 项目验证样式、声明、类型检查和生产构建。

| 任务 | 前置任务 | 完成后的行为 |
| --- | --- | --- |
| [01：嵌入组件并导入 GLB](./issues/01-import-model.md) | 无 | URL / 本地 GLB 导入，观察、取景、加载反馈与可靠切换 |
| [02：创建并恢复标注](./issues/02-create-annotations.md) | 01 | 表面点击创建，开发者获取和回填同一模型的标注 |
| [03：A 侧栏与两种模式](./issues/03-sidebar-and-modes.md) | 02 | 修改、删除、选择、只读展示、隐藏侧栏与窄屏操作 |
| [04：标注可读与遮挡](./issues/04-readable-annotations.md) | 02 | 旋转与缩放时位置对应，背面隐藏或淡化 |
| [05：独立安装接入](./issues/05-installable-package.md) | 03、04 | npm 打包产物的组件、CSS、类型和消费者构建可用 |
| [06：文档与开源上手](./issues/06-developer-onboarding.md) | 05 | README、运行示例、贡献与检查说明可实际照做 |

依赖树为 01 → 02 →（03、04）→ 05 → 06。03 与 04 在逻辑上互不阻塞；执行时可按完成情况选取已解除阻塞的任务。

每个任务都交付可演示或可验证的完整使用行为，含相应公开接口、必要类型和行为验收。正式实现重新编写；原型继续保留在专用分支作为 primary source。

发布方式：审阅验收入口和拆分后，在已确认的 GitHub Issues 中发布正式规格及六张独立任务，关联原型记录，并使用原生阻塞关系；不可用时用正文说明阻塞关系。任务标记为 `ready-for-agent`。

用户已确认验收入口、六项拆分及阻塞关系。正式规格和任务已发布到 GitHub Issues。

正式规格：#2。任务：1 → #3、2 → #4、3 → #5、4 → #6、5 → #7、6 → #8。
